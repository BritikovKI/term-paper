\section{Preliminaries}
\label{Sec:Preliminaries}
This section introduces the definitions and formalisms used in the paper. 
Logical formulas in our approach are defined over the theory of Linear Integer Arithmetic (LIA).
We use $X,X'$ to denote set of variables (unprimed and primed), where $X' = \{v'| v \in X\}$, $X$ is a set of state variables, and $X'$ is a set of next state variables.
If there are variables of many different states in the formula we use superscript notation, e.g., $X^{(0)}, \dots, X^{(n)}$ .
$s, s'$ are used to denote the specific states in the system.

%

The formula defined over $X$ is a state formula, and a formula over $X \cup X'$ is a transition formula.
State formulas encode a set of states, and transition formulas define binary relations over the state.
Using $Id(X,X')$ we denote a transition formula $X = X'$

\paragraph{Transition systems and Safety Problem} 
A \emph{transition system} (TS) is defined as $T = \tuple{ Init, Tr, X }$, where $Init$ is a formula encoding the initial states of the system, $Tr$ is a transition formula, which encodes the transition relation, and $X$ is a set of system's variables.
With respect to transition system, a \emph{transition trace} is a formula encoding multiple consecutive transitions: $Tr^{n}(X^{(0)}, \dots, X^{(n)}) = Tr(X^{(0)}, X^{(1)}) \land \dots \land Tr(X^{(n-1)}, X^{(n)})$.
We write $Tr^{n}(X^{(0)},\dots,X^{(n)})$ for this conjunction throughout the paper.
A \emph{safety problem} is defined as  $P = \tuple{ Init, Tr, Bad, X }$ where $Bad$ is a formula that represents states that violate the safety property.

%

Safety verification amounts to determining whether there exists a transition trace that reaches a $Bad$ state from the initial states.
If TS is safe, then there exists an inductive invariant $Inv(X)$ s.t. (1) $\forall X. Init(X) \rightarrow Inv(X)$, (2) $\forall X. Inv(X) \land Tr(X,X') \rightarrow Inv(X')$, and (3) $\forall X. Inv(X) \rightarrow \lnot Bad(X)$.
We use $safetyCheck(P)$ to denote a safety verification procedure that returns a tuple $\tuple{SAFE, Inv(X)}$ if TS is safe ($Inv(X)$ is inductive invariant).
Otherwise, if TS is unsafe it returns $\tuple{UNSAFE, Tr^{n}(X^{(0)}, \dots, X^{(n)})}$, such that: 

$$\exists \dots, X^{(n)}. Init(X^{(0)}) \land Tr^{n}(X^{(0)}, \dots, X^{(n)}) \land Bad(X^{(n)})$$

\paragraph{Nontermination}
A transition system is called \textbf{nonterminating} if there exists an initial state, from which it is possible to take any number of transitions:
$$\exists X. Init(X) \land \forall n \in \mathbb{N}.  \exists \dots, X^{(n)}. Tr^{n}(X, X^{(1)}, \dots, X^{(n)})$$

\begin{definition}[Recurrent Set]
    For a Transition System $\tuple{ Init, Tr, X }$, a formula $N(X)$ is a recurrent set if: (1) $\forall X. N(X) \rightarrow \exists X'. Tr(X,X') \land N(X')$,  (2) $\exists X. N(X) \land Init(X)$.
\end{definition}

Transition System is nonterminating if there exists a \emph{recurrent set}. 
Intuitively, every state in the recurrent set has a successor that also lies in the recurrent set, and the recurrent set contains some of the 
initial states.

\paragraph{Termination} A TS is called \textbf{terminating} if for every initial state it is possible to take only limited number of transitions.
For a Transition System $\tuple{ Init, Tr, X }$:
$$\forall X. Init(X) \rightarrow \exists n \in \mathbb{N}. \lnot \exists \dots, X^{(n)}. Tr^{n}(X, X^{(1)}, \dots, X^{(n)})$$
% Termination can be proven by synthesizing a \emph{ranking function}~\cite{DBLP:conf/vmcai/PodelskiR04}, mapping states to a well-founded domain, such that its value strictly decreases along every transition.

\begin{definition}[Transition invariant]
    For a Transition System $\tuple{ Init, Tr, X }$, $TrInv(X,X')$ is a transition invariant if: $\forall X,X'. R(X) \land Tr^+(X,X') \rightarrow TrInv(X,X')$, where $Tr^+(X,X')$ is the transitive closure of $Tr(X,X')$
    and $R(X)$ is a formula encoding all the states, reachable within transition system.
\end{definition}

\begin{definition}[Well-founded Relation]
    A binary relation $R \subseteq D \times D$ over a domain $D$ is \emph{well-founded} if there exists no infinite descending chain:
    $$\nexists\, d_0, d_1, d_2, \dots \in D.\ 
      \forall i \in \mathbb{N}.\ (d_i, d_{i+1}) \in R$$
\end{definition}

A relation $R(X,X')$ can be proven to be well-founded~\cite{DBLP:conf/vmcai/PodelskiR04} by synthesizing a ranking function $f(X)$, such that
 $\forall X,X'. R(X,X') \rightarrow f(X) > f(X')$.

\emph{Transition invariant}~\cite{DBLP:conf/lics/PodelskiR04} can be used to prove termination.
If transition invariant is a finite union of well-founded relations (for example if every separate relation has a ranking function), 
then the Transition System terminates.
\begin{definition}[Disjunctively well-founded relation]
    A relation $D(X,X') = \cup_{i = 0}^{n} R_i(X,X')$ is called disjunctively well-founded if every relation $R_i(X,X')$ is well-founded. 
\end{definition}

\paragraph{Craig Interpolation} An abstraction technique~\cite{Craig_1957}, such that
given two formulas $A$ and $B$ such that $A \land B$ is unsatisfiable, a Craig interpolant for $A$ and $B$ is a formula $I$ such that $A \rightarrow I$, $I \land B$ is unsatisfiable, and $I$ only contains the symbols, common to both $A$ and $B$.
We use $Itp(A, B)$ to denote the computation of the Craig interpolant for $A$ and $B$.

\subsection{Proving Nontermination via Safety}
Many techniques reduce termination and nontermination analysis to safety verification.
One notable approach is the reduction of nontermination to safety, which constructs strong recurrent sets via inductive invariants~\cite{DBLP:conf/tacas/ChenCFNO14}.
It was introduced in the context of proving nontermination of programs with loops, and can be extended for the Transition Systems.
This approach is described below.

\begin{definition}[Sink states]
    For a Transition System $\tuple{ Init, Tr, X }$, a state $s$ is a sink state, if $\lnot \exists s'. Tr(s,s')$.
    We use $Sink(X)$ to denote the formula encoding all sink states in the transition system, such that $\forall X. Sink(X) \leftrightarrow \lnot \exists X'. Tr(X,X')$
\end{definition}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{Figures/SNA.pdf}
    \caption{Execution flow of Safety-based Nontermination analysis, cf~\cite{DBLP:conf/tacas/ChenCFNO14}}
    \label{fig:SNA}
\end{figure}

The Algorithm~\ref{Alg:SNA} for Safety-based Nontermination analysis (SNA) takes a safety problem $P = \tuple{Init, Tr, Sink, X}$, where $Sink(X) = QE(\exists X'. \lnot Tr(X,X'))$.
As an output, it returns NONTERM if the system is nonterminating, and UNKNOWN, if the algorithm cannot conclude the analysis. 
The Figure~\ref{fig:SNA} contains the execution flow of Safety-based Nontermination analysis.
% c.f

\begin{algorithm}[t!]
	\SetInd{0.5em}{0.5em}
    \DontPrintSemicolon
    \small
    \newcommand{\Continue}{\textbf{continue}}
    \caption{Nontermination analysis using Safety (SNA), cf~\cite{DBLP:conf/tacas/ChenCFNO14}}
    \label{Alg:SNA}
    \SetKwInOut{Input}{Input}
    \SetKwInOut{Output}{Output}
    \Input{Safety problem $P = \tuple{Init, Tr, Sink, X}$}
    \Output{NONTERM $\mid$ UNKNOWN}
    % \SetKwFunction{FAnalyze}{Analyze}
    % \SetKwProg{Fn}{Function}{:}{}
    % \nonl\Fn{\FAnalyze{$P$}}{
        \While{$\top$}{
            $\tuple{res,\phi = Tr^{n}\ or\ Inv} \gets safetyCheck(P)$\\
            \label{SNA:UNSAFE}\eIf{$res = UNSAFE$} {
                $P \gets \blockTerm(P, \phi)$ \label{SNA:BLOCKTERM}
            }
            {
                \label{SNA:SAFE}\If{$\forall X. \phi(X) \rightarrow \exists X'. Tr(X,X')$} {
                    \Return NONTERM \label{SNA:RECURRENT}
                }
                \Return UNKNOWN
            }
            
        }
    % }
    
\end{algorithm}
% A

The approach executes a safety check of $P$ in a loop.
At line~\ref{SNA:UNSAFE}, the algorithm handles the case when $P$ is unsafe (i.e., exists a trace reaching sink states).
In this case, the algorithm calls the $\blockTerm$ procedure (Algorithm~\ref{Alg:SNABlock}), which refines the safety problem $P$ by blocking the states that determenistically
lead to the sink states, updating the Safety problem.
$\blockTerm$ is described in detail below.
In the case when $P$ is safe, algorithm checks if inductive invariant $Inv(X)$ is a recurrent set (line~\ref{SNA:SAFE}).
The check at line~\ref{SNA:SAFE} can be done, using quantifier elimination to eliminate $X'$, checking if the following formula
is unsatisfiable:
$$\exists X. \phi(X) \land \lnot QE(\exists X'. Tr(X,X')).$$
If $Inv(X)$ is a recurrent set, then the transition system is nonterminating.
Otherwise, algorithm returns UNKNOWN, as the analysis is inconclusive.

\begin{algorithm}
    \SetInd{0.5em}{0.5em}
    \DontPrintSemicolon
    \small
    \newcommand{\Continue}{\textbf{continue}}
    \caption{\blockTerm(P, $Tr^{n}$), cf~\cite{DBLP:conf/tacas/ChenCFNO14}}
    \label{Alg:SNABlock}
    \SetKwInOut{Input}{Input}
    \SetKwInOut{Output}{Output}
    \SetKwInOut{Data}{Data}
    \Input{Safety problem $P = \tuple{Init, Tr, Sink, X}$, terminating trace $Tr^{n}(X^{(0)}, \dots, X^{(n)}) = Tr(X^{(0)}, X^{(1)}) \land \dots \land Tr(X^{(n-1)}, X^{(n)})$}
    \Output{Refined safety problem $P' = \tuple{Init', Tr', Sink', X}$}
    \Data{$S_0,\dots,S_n$ - formulas which encode states within trace reachable in 0, ..., n transitions}

    % \SetKwFunction{FBlock}{BlockTerm}
    % \SetKwProg{Fn}{Function}{:}{}
    % \Fn{\FBlock{$P, trace$}}{
    \SetAlgoFuncName{BlockTerm}{autoref name}
        $i \gets n-1$ \\
        \While{$i > 0$}{
            $S_{i+1}(X^{(i+1)}) \gets QE(\exists X^{(0)},\dots, X^{(i)},  X^{(i+2)}, \dots, X^{(n)}.$ $Init(X^{(0)}) \land Tr^{n} \land Sink(X^{(n)}))$ \\
            $S_{i}(X^{(i)}) \gets QE(\exists X^{(0)},\dots, X^{(i-1)},  X^{(i+1)}, \dots, X^{(n)}.$ $Init(X^{(0)}) \land Tr^{n} \land Sink(X^{(n)}))$ \\
            \label{SNA:BLOCK} \If{SAT ? $S_i(X) \land Tr(X,X') \land \lnot S_{i+1}(X')$}{
                $Tr'(X,X') \gets Tr(X,X') \land \lnot S_{i+1}(X')$ \label{SNA:TRBLOCK}  \\
                \Return $\tuple{Init, Tr', Sink, X}$
            }
            $i \gets i - 1$   
        }
        $Init'(X^{(0)}) \gets Init(X^{(0)}) \land \lnot QE(\exists X^{(1)}, \dots, X^{(n)}. Init(X^{(0)}) \land Tr^{n} \land Sink(X^{(n)}))$  \label{SNA:BLOCKINIT} \\
        \Return $\tuple{Init', Tr, Sink, X}$
    % }
\end{algorithm}

The $\blockTerm$ procedure takes as input a safety problem $P$ and a trace formula $Tr^{n}(X^{(0)}, \dots, X^{(n)})$ such that $Init(X) \land Tr^{n}(X, \dots, X') \land Sink(X')$ is satisfiable.
The algorithm iteratively analyses the transition trace from the sink. 
It checks if it is possible to take a transition that does not lead to the sink states.
Line~\ref{SNA:BLOCK} checks if $S_i(X) \land Tr(X,X') \land \lnot S_{i+1}(X')$ is satisfiable, sending a query to the SMT solver.
If it is possible, then the algorithm blocks the states that deterministically lead to the sink states at line~\ref{SNA:TRBLOCK}.
Otherwise, if such states do not exist, the algorithm blocks the subset of initial states that deterministically lead to the sink states (line ~\ref{SNA:BLOCKINIT}).


\begin{lemma}\label{Lem:NONTERM}
    If Algorithm~\ref{Alg:SNA} returns NONTERM, then the transition system is nonterminating, cf~\cite{DBLP:conf/vmcai/PodelskiR04}.
\end{lemma}
% Example can be as a core for the gap that you need to feel in the paper
% Mention why do we need recurrent sets and why it is important!!!
% One var instead of 3 - integer or smth... 
% Motivation example!!!! - why the original algorithm is problematic!!!
% Concentrate example on section 3!
% Make an example for section 4
% \begin{example}\label{Ex:SNA}
%     Consider a transition system $TS = \tuple{Init, Tr, X}$ with 
%     boolean variables $X = \{X\}$ and: $Init(X)  \triangleq \top$,
%         $Tr(X, X') \triangleq X' = X - 2 \land X \neq 0 \land X < 10$. 
%     The sink states are computed as 
%     $Sink(X) \triangleq X = 0 \lor X \geq 10$.
%     The initial safety problem is $P = \tuple{Init, Tr, Sink, X}$.
%     This transition system is nonterminating and algorithm would proceed as follows.

%     First, SNA would detect that sink states are the part of initial states $X = 0$,
%     and would call \blockTerm, updating initial states: $Init(X) \triangleq X \neq 0$.
%     Afterwards, SNA would detect a counterexample $Tr^1$, detecting that sink states 
%     can be reached within 1 iteration.
%     \blockTerm will once again block the initial states that reach termination: 
%     $Init(X) \triangleq X \neq 0 \land X \neq 2$.
%     Execution of the SNA would proceed, until it blocks all of the initial states that lead to 
%     termination: $Init(X) \triangleq X \neq 0 \land \dots \land X \neq 8$.
%     Afterwards, SNA safety check would conclude safety, returning safety invariant:
%     $Inv(X) = X<10 \land X \neq 0 \land \dots \land X \neq 8$.
%     This is recurrent set, so SNA would conclude nontermination.

%     However, consider a small change to the Transition System.
%     Particularly, $Tr(X, X') \triangleq X' = X - 2 \land X > 0 \land X < 10$.
%     This change would render transition system is terminating, but, 
%     Algorithm~\ref{Alg:SNA} is unable to detect termination.
%     It would loop infinitely, blocking initial states that lead to termination.

%     However, SNA detects terminating traces during execution.
%     Intuition is to use such traces to prove the termination of transition systems.
%     If such traces are overapproximated, it is possible to construct a transition invariant,
%     which will witness the termination.
%     Particularly, in the case of example the well-founded transition invariant $TrInv(X,X') = X' < X \land X > 0$
%     would witness termination.
% \end{example} 
% \begin{example}\label{Ex:SNA}
%     Consider a transition system $TS = \tuple{Init, Tr, X}$ with 
%     boolean variables $X = \{X\}$ and: $Init(X)  \triangleq \top$,
%         $Tr(X, X') \triangleq (X' = X - 1 \lor X' = X - 2) \land X \neq 0$. 
%     The sink states are computed as 
%     $Sink(X) \triangleq X = 0$.
%     The initial safety problem is $P = \tuple{Init, Tr, Sink, X}$.
%     This transition system is nonterminating and algorithm would proceed as follows.

%     First, SNA would detect that sink states are the part of initial states $X = 0$,
%     and would call \blockTerm, updating initial states: $Init(X) \triangleq X \neq 0$.
%     Afterwards, SNA would detect a counterexample $Tr^1$, detecting that sink states 
%     can be reached within 1 iteration.
%     \blockTerm will block the transition, blocking branch that reaches sink state: 
%     $Tr(X,X') \triangleq Tr(X,X') \land X' \neq 0 $.
%     Execution of the SNA would proceed, safety check concluding safety for the updated Transition System:
%     $Inv(X) = X \neq 0$.
%     This invariant is not a recurrent set, so SNA would return UNKNOWN.

%     However, consider a small change to the Transition System.
%     Particularly, $Tr(X, X') \triangleq (X' = X - 1 \lor X' = X - 2) \land X > 0$.
%     This change would render transition system terminating, but, 
%     Algorithm~\ref{Alg:SNA} is unable to detect termination.
%     It would loop infinitely, blocking initial states that lead to termination.

%     However, SNA detects terminating traces during execution.
%     Intuition is to use such traces to prove the termination of transition systems.
%     If such traces are overapproximated, it is possible to construct a transition invariant,
%     which will witness the termination.
%     Particularly, in the case of example the well-founded transition invariant $TrInv(X,X') = X' < X \land X > 0$
%     would witness termination.
% \end{example}
\begin{example}\label{Ex:SNA}
    Consider a transition system $TS = \tuple{Init, Tr, X}$ with 
    $X = \{v\}$, where $v$ is an integer variable, $Init(X)   \triangleq \top$, and
    $Tr(X, X') \triangleq (v' = v - 1 \lor v' = v - 2) \land v \neq 0$.
    The sink states can be encoded as $Sink(X) \triangleq v = 0$.
    The initial safety problem is $P = \tuple{Init, Tr, Sink, X}$.

    \textit{Iteration 1.} $safetyCheck(P)$ returns UNSAFE: the sink 
    state $v = 0$ is reachable from $Init$ in zero 
    transitions. 
    \blockTerm\ blocks this state  $Init(X) \;\gets\; v \neq 0$.

    \textit{Iteration 2.} $safetyCheck(P)$ returns UNSAFE with a 
    trace $Tr^1(X^{(0)}, X^{(1)})$. \blockTerm\ detects that $\exists X^{(0)}, X^{(1)}. 
    Init(X^{(0)}) \land Tr^1(X^{(0)}, X^{(1)}) \land \lnot Sink(X^{(1)})$. 
    It blocks the states $X^{(1)}$ which necessarily lead to termination: 
    $Tr(X, X') \;\gets\; Tr(X, X') \land v' \neq 0$.

    \textit{Iteration 3.} $safetyCheck(P)$ returns SAFE with 
    inductive invariant $Inv(X) \equiv v \neq 0$. 
    The invariant $Inv$ is a recurrent set as it is possible to take
    a transition from all states within invariant, particularly $v=1$ or 
    $v=2$. 
    Algorithm~\ref{Alg:SNA} therefore returns \textsc{NONTERM}.

    \textit{Motivation for transition invariant-based termination analysis.} Consider a 
    small modification: $Tr(X, X') \triangleq (v' = v - 1 \lor 
    v' = v - 2) \land v > 0$. This renders $TS$ terminating, 
    since $v$ is strictly positive and decreases every step. 
    However, Algorithm~\ref{Alg:SNA} cannot detect termination by design, in the 
    case of this particular example it would return $UNKNOWN$, after updating 
    transition to $Tr(X,X') = Tr(X,X') \land v' > 2$. 
    Crucially, terminating traces \emph{are} detected during the execution of
    the algorithm. 
    The key insight is to over-approximate these traces via 
    interpolation, to construct a transition invariant witnessing termination. 
    In this example, the disjunctively well-founded transition invariant 
    $TrInv(X, X') \triangleq v' < v \land v > 0$ covers all reachable states 
    and witnesses termination of $TS$.
\end{example}